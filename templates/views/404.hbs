<!DOCTYPE html>

<html>
    <head>
        <link rel="stylesheet" href="/CSS/style.css">
    </head>

    <body>
        {{>header}}
        <p>{{errorMessage}}</p>
        {{>footbar}}
    </body>
</html>